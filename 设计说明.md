# dsh-prompt-star · 设计说明

> 更新：2026-09-08 · 初稿（来自 2026-09-08 与 AI 的设计讨论）· 第二轮补充用户决策

## 一句话

一个 DeepSeek Harness (DSH) 插件：在对话输入框旁放一个 **⭐ 星星按钮**。你随手写完草稿点一下星星，它读懂你的意图、匹配常用工作提示词模板、读取当前项目说明文件作为上下文，生成一版更完整高效的提示词，直接填入输入框；不满意可按 Ctrl/Cmd+Z 撤回原草稿。

## 为什么做

- 公司引入 AI 考核，核心看"提示词是否省 token、写得是否清楚"——这个工具是把"高效提示词能力"产品化，考核可现场演示，也是作品（VoiceAgent 之后第二个）
- 之前试装 `dsh-ai-prompt-optimizer`（第三方）失败（按钮不显示），决定自己写，可控
- 已有 `@necokeine/dsh-git`（AI 提交信息）可参考 DSH 插件写法

## 用户流程

```
你打字（随手草稿）: "帮我提个需求，做个登录页"
      ↓ 点 ⭐
1. 读懂意图 → 匹配模板骨架（提需求 / 改bug / 写信…）
2. 探测当前项目说明/相关文件 → 读入作上下文（没什么不能读的）
3. 模型基于草稿一步步自主生成一版完整提示词 → 直接填入输入框
4. 满意就发送；不满意就 Ctrl/Cmd+Z 一键撤回原草稿
```

## 核心决策（已定）

| 项 | 决策 |
|---|---|
| 形态 | DSH 插件（client 星星按钮 + host 调模型/读文件），独立项目开发后 `dsh plugin add` |
| 项目说明文件 | **全认**：CLAUDE.md / AGENTS.md / .cursorrules / README.md，按优先级找，有哪个读哪个 |
| 没有说明文件时 | 弹窗问"要我帮你生成 CLAUDE.md 吗" → 确认后读项目结构自动生成草稿 → 用户编辑保存 |
| 模板角色 | 不是"手动套用框架"，是给模型当**参考骨架**（提示词该长这样），模型结合项目信息现场生成 |
| 生成结果交互 | **直接生成并填入**，步骤最少、操作方便；不满意用 Ctrl/Cmd+Z 撤回原草稿（不专门做"恢复草稿"按钮，不做 diff 对比编辑） |
| 文件读取边界 | **不做内容白名单**：项目里相关文件都能读作上下文；仅为性能跳过 node_modules/.git/构建产物等超大无关目录 |
| 生成方式 | 由模型基于草稿**一步步自主生成/补齐**（用户不手写框架），用户只确认最终结果；未指定的细节由开发者自行决定 |
| 模型 | 复用 DSH 当前已配置的默认模型（不额外要 API key），生成一步消耗少量 token，可接受 |
| 范围 | MVP 最小可用优先，模板库/意图精确匹配后续迭代 |

## 项目说明文件（CLAUDE.md）建议章节

项目简介 · 技术栈 · 目录结构 · 常用命令 · 代码规范 · 注意事项/坑

## 首批模板（骨架）

- 提需求
- 改 bug
- 写信

（用户可自行扩展，模板库后续迭代成可视化编辑）

## 技术要点

- host 端：读 workspace 项目文件、探测说明文件、调模型（复用 `agentDefaultModel` 当前选择）
- client 端：`conversation.input` slot 挂 ⭐ 按钮 + 弹窗组件
- 参考：`dsh-ai-prompt-optimizer`（结构可借鉴）、`@necokeine/dsh-git`（同型插件）
- 隐私注意：本地生成、不上传代码（用户项目多涉内网/涉密）

## 用户决策（2026-09-08 · 第二轮补充）

- 结果展示：**直接生成结果**，操作越简单越好；不做 diff / 对比编辑器。✅ 已确认（不弹窗，直接填入输入框）
- 撤回：用 **Ctrl/Cmd+Z** 回到原草稿，不单独做"恢复草稿"按钮。✅ 已确认
- 读取边界：**没有什么不能读的**，项目相关文件都能读作上下文；仅跳过超大无关目录。✅ 已确认（保留排除 node_modules/.git/构建产物等）
- 生成方式：由模型**逐步自主生成/补齐**，用户不手写，只确认最终结果；其余细节开发者自行拍板。

## 尚未细化（写成代码前再定）

- client ↔ host 通信协议（RPC 调用名 + 消息体）
- 说明文件/上下文探测的时机与缓存策略
- 模型调用失败 / 超时 / 生成不理想的兜底
- 模板存放格式（`templates/*.md` + 头部 YAML）与加载方式
- 衡量指标（生成前后 token 对比、耗时）与现场 Demo 脚本
- 插件发布、版本管理与 DSH 主版本兼容性

## 当前实现状态（已落地代码）

已按确认的决策生成本插件组合包（位于 `packages/extensions/prompt-star/`，按 harness 原生结构组织）：

- `package.json` — `dsh.bundle.patch` + `dsh.client` manifest（组合包 + client 浏览器 bundle）
- `cordis.patch.yml` — host 插件行
- `src/index.ts` — host `PromptStarService`（`TypertRemoteService` + `@Remote('generate')`）
- `src/host/generate.ts` — 探测说明文件 + `ctx.llm.stream` + `BlockAssembler`
- `src/client/index.ts` + `src/client/StarButton.tsx` — 注册进 `conversation.input.right` 的 ⭐ 按钮，`useInput()` 读草稿 / `inputActions.setDraft()` 写入
- `tsdown.config.ts` — `clientBundle('dsh-prompt-star', ['src/index.ts'], { hostPhase: true })`
- `templates/*.md` — 提需求 / 改bug / 写信 / 通用 骨架
- `.github/workflows/build.yml` — CI：本地零构建，CI 上 checkout harness + 叠加 + 构建 + 发布
- `README.md` — 架构、构建/安装、待核验点

**发布模型**：client bundle 无法独立构建（`clientBundle()` 依赖 harness 内部模块），必须放在 harness workspace 里构建 → **在 CI 上构建，你在本地零下载/零构建**，只 `dsh plugin add` 安装成品（详见 `README.md`）。

**尚需在 DSH 开发环境核验**（离线无法验证，首次 CI 运行须实测校准）：client bundle 构建管线与 `DSH_BUILD_FACE` 传递、tsconfig 归入 host/client program、`ctx.remote.promptStar` 命名空间类型、`dsh-llm` 的 `purpose` 加 `'prompt-star'` 补丁、workspace 根目录取真实会话根、UI 实际渲染位置。详见 `README.md`。 

## 避坑提醒（来自实际踩坑）

- 第三方插件按钮不显示的常见原因：client bundle 未加载 / host remote 未激活 / 浏览器缓存旧 client manifest → 硬刷新 (Cmd+Shift+R) 或查服务端日志
- 插件需自带 `dsh.bundle` 声明才会被 `dsh plugin add` 自动加入 bundles 列表
